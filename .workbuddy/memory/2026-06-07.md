# 2026-06-07

## social-feed 包架构重构（方案 C：完整服务层）
- 从扁平 14 个 .rs 重构成 4 层目录架构：
  - `types/` — models.rs, error.rs, config.rs（零 SDK 依赖纯数据）
  - `core/` — feed.rs（SocialFeed）, profile.rs, social.rs, interaction.rs, helper.rs（业务实现）
  - `services/` — timeline.rs, cache.rs, pagination.rs, search.rs（独立服务）
  - `utils/` — validators.rs, text.rs, images.rs（纯函数工具）
- lib.rs 重写为模块声明 + 公开 API re-export（Moment, UserProfile, SocialFeed, SocialFeedError, Config, ConfigBuilder）
- 向后兼容：测试中 `use crate::Moment` 等路径通过 re-export 继续工作
- 拆分了原 utils.rs → validators.rs + text.rs；原 timeline.rs 中的图像提取函数 → utils/images.rs
- 更新 README.md 文件结构图

## social-feed 文档清理
- 逐个与源码比对 16 个根目录 .md 文件
- 删除 14 个过期文档（开发过程日志/一次性快照/错误签名/过期测试计数）
- 保留 3 个：README.md（入口）、API_CHANGES.md（Error 迁移参考，全签名正确）、TESTING.md（测试指南）
- 修复 TESTING.md 中过期的文件结构图和测试计数（56→73）

## social-feed 5 个新服务模块接入
- media.rs: 修复 thumbnail_url 字段与方法命名冲突 → build_thumbnail_url
- aggregation.rs → timeline.rs: fetch_room_moments 改用 AggregationCache::update_batch 替代手写 HashMap 计数
- aggregation.rs → interaction.rs: like() 后 increment_likes, comment() 后 increment_replies, forward() 后 increment_forwards
- quote_forward.rs → interaction.rs: forward() 使用 ForwardMetadata 生成 formatted_body + plain_body 双格式原文引用
- rate_limit.rs → core: SocialFeed 加 rate_limiter 字段，like/comment/forward/post_moment/follow 入口前调用 wait_until_allowed()
- feed.rs: SocialFeed 新增 aggregation_cache(Arc<AggregationCache>) + rate_limiter(Arc<RateLimiter>) 字段

## social-feed 全量代码审查 + bug 修复 + 文档更新
- 全量扫描 25 个源文件（~2500 行），发现 10 个 bug / 9 处死代码 / 7 个模式问题
- 修复 6 个关键 bug：
  1. profile.rs: MxcUri 类型错误（try_into 返回 owned，不是 &MxcUri）
  2. profile.rs: update_bio/update_location 竞态（互相覆盖 topic）→ 抽取 rebuild_topic()
  3. interaction.rs: forward() 中 ForwardMetadata 全空字段 → 回退到纯文本 + TODO
  4. interaction.rs: increment_forwards("", event_id) 空 room_id → 暂时不计数 + TODO
  5. timeline.rs: post_moment 将 MD 当 HTML 传 text_html() → 改为 text_plain
  6. utils/text.rs: truncate_text 用字节切片可能 panic 在 UTF-8 边界 → 改用 chars().take()
- 更新 README.md：架构图/API 示例/新增服务模块/已知限制
- 更新 API_CHANGES.md：timeline/user_moments 签名从 &self → &mut self
